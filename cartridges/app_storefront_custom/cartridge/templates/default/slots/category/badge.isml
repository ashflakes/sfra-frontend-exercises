   <isif condition="${slotcontent != null}">
      <isloop items="${slotcontent.content}" var="category">
         <isif condition="${session.custom.categoryId === category.ID}">
            <span class="badge">Clothing<br />Badge</span>
         </isif>
      </isloop>
   </isif>




